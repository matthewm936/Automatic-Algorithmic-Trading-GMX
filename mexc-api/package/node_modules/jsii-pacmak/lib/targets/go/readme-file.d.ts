import { EmitContext } from './emit-context';
export declare class ReadmeFile {
    private readonly packageName;
    private readonly document;
    constructor(packageName: string, document: string);
    emit({ code }: EmitContext): void;
}
